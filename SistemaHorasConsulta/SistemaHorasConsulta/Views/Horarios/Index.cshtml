@model IEnumerable<ITCR.DATIC.SistemaHorasConsulta.Modelo.Horario>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Principal.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoraInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraFinal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoraInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoraFinal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdHorario }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdHorario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdHorario })
        </td>
    </tr>
}

</table>
