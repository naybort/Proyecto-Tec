@model IEnumerable<ITCR.DATIC.SistemaHorasConsulta.Modelo.Tematica>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Principal.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTematica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTematica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTematica }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTematica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTematica })
        </td>
    </tr>
}

</table>
